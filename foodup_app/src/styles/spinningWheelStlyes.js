import { StyleSheet } from "react-native";

export default SpinningWheelStyles = StyleSheet.create({
    header:{
        fontSize: 36,
        fontWeight: 'bold',
        marginBottom: 30,
    },
    subtitle: {
        fontSize: 16,
        fontWeight: '300',
        marginTop: 30,
    },
    subtitleRow: {
        flexDirection: 'row',
        
    }
})