import { StyleSheet } from "react-native/";
export const DeleteBox = StyleSheet.create({
    deleteboxContainer:{
        justifyContent: 'center',
        alignItems: 'center',
        padding: 20,
        borderRadius: 5,
        
    },
    deleteboxText: {
        color: 'white',
        fontWeight: 'bold',
    }
})
