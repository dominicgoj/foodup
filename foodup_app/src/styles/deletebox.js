import { StyleSheet } from "react-native/";
export const DeleteBox = StyleSheet.create({
    deleteboxContainer:{
        justifyContent: 'center',
        alignItems: 'center',
        padding: 8,
        borderRadius: 5,
        
    },
    deleteboxText: {
        color: 'white',
        fontWeight: 'bold',
    }
})
