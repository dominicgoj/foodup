import { StyleSheet } from "react-native/";
export const MapStyles = StyleSheet.create({
    calloutContainer: {
        justifyContent: 'center',
        alignItems: 'center'
        
    },
    restaurantNameText: {
        fontSize: 12,
        fontWeight: 'bold',
    }

})
